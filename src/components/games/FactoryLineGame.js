/**
 * @description Game of a Factory line that moves across, press button to make Robot when parts align with machine
 * @returns {HTMLCanvasElement} Factory Game UI
 */
const FactoryLineGame = () =>  (
  <canvas id='factoryline-game'/>
);

export default FactoryLineGame;
